name: Agent-friendly score diff

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  score-diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: hsnice16/agent-friendly-action@9b3a096a3f7f3de005216abd14bacfc4d89e8f2a # v0
        with:
          agents-badge-token: ${{ secrets.AGENTS_BADGE_TOKEN }}
